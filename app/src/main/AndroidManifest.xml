<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.medvedev.app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity android:name="com.medvedev.presentation.activities.cars.CarListActivity" />
        <activity android:name="com.medvedev.presentation.activities.cars.CarEditActivity" />
        <activity android:name="com.medvedev.presentation.activities.cars.CarDetailsActivity" />

        <activity android:name="com.medvedev.presentation.activities.drivers.DriverListActivity" />
        <activity android:name="com.medvedev.presentation.activities.drivers.DriverEditActivity" />
        <activity android:name="com.medvedev.presentation.activities.drivers.DriverDetailsActivity" />

        <activity android:name="com.medvedev.presentation.activities.fuel.CarFuelListActivity" />
        <activity android:name="com.medvedev.presentation.activities.fuel.CarFuelEditActivity" />
        <activity android:name="com.medvedev.presentation.activities.fuel.CarFuelDetailsActivity" />

        <activity android:name="com.medvedev.presentation.activities.docs.NormativeDocsActivity" />
        <activity android:name="com.medvedev.presentation.activities.docs.Resolution3Activity" />
        <activity android:name="com.medvedev.presentation.activities.docs.Resolution141Activity" />

        <activity
            android:name="com.medvedev.presentation.activities.MechanicActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>